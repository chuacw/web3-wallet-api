// The following should be import { getWallets } from "web3-walet-api";
import { getWallets } from "../dist/web3-wallet-api";

// The following code needs to be executed while in a browser
const wallets = getWallets();
